// Define and export the HeaderComponent function
export default function HeaderComponent(){
    return(
        // Container for the header with a specific layout class
        <div className="header-layout">
            {/* Title text with a specific layout class */}
            <p className="title-layout">Electricity Board Data</p>
        </div>
    )
}